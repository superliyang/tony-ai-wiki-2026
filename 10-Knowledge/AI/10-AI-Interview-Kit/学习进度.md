---
title: AI Interview Kit 学习进度
type: progress
status: active
updated: 2026-04-29
---

# AI Interview Kit 学习进度

## Current

- current_topic: detailed AI interview deep-dive points
- current_goal: extend the god-view interview framework into concrete second-round and third-round deep-dive points across principles, training, inference, RAG, agent, eval, security, product, and organization

## Completed

- Built the first full interview submodule with `专题总览 / 学习进度 / 恢复笔记`.
- Added a true interview opening layer with `开场与项目深挖题库`, so the pack now starts from self-introduction, project deep dive, owner boundary, failure handling, and senior-level positioning.
- Added a `团队与业务沟通题库`, so the pack now covers management-facing and cross-functional questions instead of stopping at pure technical depth.
- Added `资深候选人表达总纲`, so the module now teaches how to sound like a senior owner before drilling into individual topics.
- Added an expert-style detailed answer playbook so preparation can move from answer skeletons to complete interview-ready responses.
- Added follow-up drill packs: interviewer follow-up scripts, candidate counter-question templates, and common wrong-answer corrections.
- Added `面试问题导航` and `面试决策导航` so preparation can start from either technical question or architecture decision.
- Added a full knowledge-base layer with `完整面试题蓝图` plus three missing high-leverage banks: `系统设计与场景`、`成本、性能与容量规划`、`项目复盘与事故`.
- Added company-style scenario packs for `企业知识库 / AI Coding Agent / 客服 Copilot`, so the knowledge base now supports realistic company-context interview rehearsal instead of only generic question coverage.
- Added `题目覆盖审查：按五条主干与面试维度`, so the module can now judge its own coverage instead of relying on subjective confidence.
- Added `11-多模态、语音、OCR 与 Computer Use题库`, so the long-tail lane now has a first-class question bank instead of only scattered mentions.
- Added `12-产品落地、Rollout、ROI 与 Governance题库` and `13-Memory、Planning 与 Multi-Agent题库`, so two of the remaining high-leverage weak spots now also have formal question-bank coverage.
- Added `AI 面试上帝视角：从底层原理到业务落地`, `大模型与 AI 全景回答母版`, and `14-大模型与 AI 全景题库`, so the kit now has a meta-framework for answering from first principles through training, serving, RAG/Agent, eval, security, rollout, ROI, and team mechanism.
- Added `AI 面试详细考点地图：从原理到系统与组织` and `15-细节追问与深挖题库`, so the kit now also covers the detailed follow-up layer: tokenization, embedding, attention, sampling, SFT/RLHF, KV Cache, routing, RAG, tool use, sandbox, eval, prompt injection, PII, ROI, and role-specific deep dives.

## Next

- Expand the highest-value detailed follow-ups into full 2-minute model answers, especially `hallucination`, `RAG permission`, `sandbox approval`, `LLM-as-judge`, and `ROI`.
- Add role-specific god-view drill sheets later for `Research / Applied ML / AI Platform / Agent / Product Lead`.
- Add one more layer of scenario packs later for `voice/realtime`, `document AI`, and possibly `browser/computer-use`, if those become target interview lanes.
- Consider a future deep-safety pack later for `approval / telemetry / red teaming / operating model`, if targeting more security-heavy roles.
