---
title: 学习进度
type: progress
status: active
domain: AI-Engineering
current_topic: AI Coding Lab hands-on practice system
last_completed: added AI Coding Lab project pack and AI Coding 专家能力验收矩阵
next_up: run the first P0-P2 low-risk task through the lab template and score it with the acceptance matrix
updated: 2026-05-16
---

# 学习进度

## Current

- current_topic: AI Coding Lab hands-on practice system
- current_goal: turn AI Coding team rollout and governance maps into executable experiments, task records, checklists, and acceptance criteria

## What Just Got Stronger

- `AI Coding 团队落地路线图` now gives the harness branch a staged rollout path from personal trial to team governance
- `AI Coding 安全治理决策图` now gives the branch a concrete permission and control decision map for file access, commands, MCP, PR/CI, and sensitive data
- `AI Coding Lab` now gives the branch a 30-day hands-on route, task record template, expert checklist, team pilot playbook, and acceptance matrix
- `Claude Code Harness 工程实践` now gives the Claude Code branch an engineering lifecycle: intake -> repo map -> plan -> edit -> verify -> review -> commit / PR -> retrospective
- The harness branch now has a concrete product-shaped case for skill, subagent, hook, MCP, plugin, permission, audit, and release-gate decisions
- `Mac AI Expert Path` 现在不只是章节路线，而是补上了 `Mac AI 工作台`、`系统化实验路线图`、`实验记录模板` 和更清晰的本地 AI 专家路径图
- 这让 MacBook Pro Max 学 AI 的方式从“装工具 + 跑模型”升级为“提出问题 -> 跑实验 -> 记录 bad cases -> 形成选型判断 -> 做出小系统”
- 新路线明确了 `Ollama / llama.cpp / MLX-LM / PyTorch MPS` 的分工，并把 RAG、Agent、Eval、云上映射接成一条可练习的闭环
- `Agent Runtime` 这条线现在已经不只讲 architecture，还补上了 failure cases、postmortems、rollout gates 和 blast radius control
- 新增 `Plugin、Memory 与 Background Task 失效模式`，把最容易隐藏问题的三层单独拆出来讲
- 现在又把 memory 正式拆成一条子主线：`记忆架构：State、Memory、Artifact 与 Context`、`记忆写入、召回、压缩与 Consolidation`、`记忆评测、污染、遗忘与纠偏`
- `Memory Lab` 也补上了分层、写入策略、准自进化实验，以及 learnings -> promotion -> skill extraction 实验
- 现在还把最小 `self-improving-learning-ledger` skill 脚手架真正落地了：可记录 learning、生成 promotion report、产出 review-only skill stub
- 这一轮继续把它推进到了 `incident-fed eval`、`shadow rollout`，并在本地做了最小 plugin wrapper scaffold
- 现在又把它推进到了更像正式门禁的一层：consolidation、manual verify、cluster-based candidate skill、incident pack replay、expectation checks
- 这一轮又把 prompt / context / tools / CLI / skills / plugins / harness 的工程分层单独讲清楚了，方便后面继续拆细子域
- 现在又把它继续拆成了 `Prompt 到 Context`、`动作面设计`、`扩展面设计` 三个更适合工程学习和教学的子域页
- 这一轮又把全局 `当前工作台` 和 `Self-Improving Memory Lab 验收与当前边界` 补出来，跨线程恢复和插件验收边界都清楚了
- 这一轮又补出了一条更完整的 `Agent Evaluation` 子主线：新增 `Agent 效果评估：方法论、开源工具与判断框架`，把任务分层、trace grading、online feedback、release gate 和开源工具组合真正收成了工作台
- 同时补上了 `Ragas`、`Inspect AI`、`Giskard` 三张系统卡，让 agent eval 不再只停留在 `Langfuse / Phoenix / Promptfoo`
- 现在又把这条线继续落成了可以直接复用的资产：`Agent 效果评测模板`、`Agent Eval 任务包模板`、`Agent 效果评估指标清单`、`Agent Eval 任务包：Repo、Research、RAG、Tool Use`
- 现在又把它往前推进成三套半成品案例包：`Coding Agent Eval Pack`、`企业知识库 Agent Eval Pack`、`Research Agent Eval Pack`

## Why This Matters

- 现在 AI-Engineering 不再只是训练深、harness 深，而 runtime 下半场明显补齐了
- 这让 agent engineering 不只会谈 capability，还会谈失效、回滚、恢复和 rollout discipline
- 后面继续补 incident library 时，会更像持续积累的运行经验库，而不是新的概念页
- 这一轮已经把 self-improving memory 的 risk/governance 层补到位：incident library、shared-memory boundary、eval/release gate、rollback
- 更重要的是，`Memory Lab` 现在不只是“懂理论”，而是有了可回放的本地 plugin lab
- 现在 `AI-Engineering` 的评测层也更完整了：不只是通用 eval，而是明确有了 agent 任务评估、工具选型和效果判断框架
- 这让这条线第一次具备了“拿来就能开工”的形态，而不只是读完觉得有道理
- 现在你甚至已经可以不用从空白页开始搭评测，而是直接拿这三套包改成自己的任务集

## Next

1. 先打开 [[06-Projects/AI Coding Lab/项目总览|AI Coding Lab]] 和 [[06-Projects/AI Coding Lab/AI Coding Lab 30 天训练路线|AI Coding Lab 30 天训练路线]]
2. 选择一个 `P0 -> P2` 低风险任务跑通完整闭环
3. 用 [[06-Projects/AI Coding Lab/AI Coding Lab 实战任务记录模板|AI Coding Lab 实战任务记录模板]] 留证据
4. 用 [[04-Evaluation/AI Coding 专家能力验收矩阵|AI Coding 专家能力验收矩阵]] 打分，并决定下一步沉淀 skill、subagent、hook 还是 plugin
