---
title: Manual URL Inbox
type: manual-url-inbox
status: active
---

# Manual URL Inbox

把临时想让 Agent 巡检的 URL 粘到这里。下一次 Daily / Weekly 自动化会读取未完成条目，抓取标题和正文摘要，并进入分类、候选池、Study Queue 和 Review Queue。

## 待采集

- [x] 示例：把真实链接粘成这种格式，Agent 会自动抓取。

## 约定

- 未勾选的 `http` / `https` 链接会被采集。
- 已勾选 `[x]` 的链接会被跳过。
- 采集后暂时不会自动改写本文件，避免误改你的收藏列表。
